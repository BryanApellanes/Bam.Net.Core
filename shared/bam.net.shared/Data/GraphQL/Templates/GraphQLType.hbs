namespace Bam.Net.Data.GraphQL.Templates
{
    using System;
    using System.Collections.Generic;
    using Bam.Net.Logging;
    using GraphQL;
    using GraphQL.Types;

    public class {{TypeName}}Graph : ObjectGraphType<{{TypeName}}>
    {
        public {{TypeName}}Graph()
        {
{{#each Properties}}
            Field("{{PropertyName}}", p => p.{{PropertyName}}, type: typeof({{PropertyGraphType}});    
{{/each}}
        }
    }
}