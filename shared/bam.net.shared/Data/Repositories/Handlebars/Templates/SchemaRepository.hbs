/*
This file was generated and should not be modified directly
*/
using System;
using System.Collections;
using System.Collections.Generic;
using System.Reflection;
using System.Data;
using System.Data.Common;
using System.Linq;
using System.Threading.Tasks;
using Bam.Net;
using Bam.Net.Data;
using Bam.Net.Data.Repositories;
using {{BaseNamespace}};

namespace {{SchemaRepositoryNamespace}}
{
	[Serializable]
	public class {{SchemaName}}Repository: {{BaseRepositoryType}}
	{
		public {{SchemaName}}Repository()
		{
			SchemaName = "{{SchemaName}}";
			BaseNamespace = "{{BaseNamespace}}";			

{{#each Types}}
{{> SchemaRepositoryAddType}}
{{/each}}

			DaoAssembly = typeof({{SchemaName}}Repository).Assembly;
		}

		object _addLock = new object();
        public override void AddType(Type type)
        {
            lock (_addLock)
            {
                base.AddType(type);
                DaoAssembly = typeof({{SchemaName}}Repository).Assembly;
            }
        }

{{#each Types}}
{{> SchemaRepositoryMethods}}
{{/each}}

	}
}																								
