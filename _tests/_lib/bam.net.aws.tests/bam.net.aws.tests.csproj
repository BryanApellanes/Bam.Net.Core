<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net5.0</TargetFramework>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
      <OutputPath>\opt\bam\build\tests\debug\bam.net.aws.tests\</OutputPath>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
      <OutputPath>\opt\bam\build\tests\release\bam.net.aws.tests\</OutputPath>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="bam.net.aws" Version="1.0.*" />
      <PackageReference Include="bam.net.core" Version="1.0.*" />
    </ItemGroup>

</Project>
